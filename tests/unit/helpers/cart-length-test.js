import { cartLength } from 'online-marketplace/helpers/cart-length';
import { module, test } from 'qunit';

module('Unit | Helper | cart length');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cartLength([42]);
  assert.ok(result);
});
